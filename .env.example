OPENAI_API_KEY=sk-...your-key-here...

# Public-facing admin / front-desk phone, given to guests for anything
# outside the FAQ scope. Also used to gate the /testrule command.
# Optional — if unset, the bot falls back to the hostel email.
ADMIN_PHONE_NUMBER=+34 600 000 000
